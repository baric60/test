// import { catOptions, head } from "fp-ts/lib/Array";
// import { Option, some, none } from "fp-ts/lib/Option";

// const option1: Option<number> = some(13);
// const option2: Option<number> = some(2);

// const array: Array<Option<number>> = [option1, none, none, option2];
// const filteredArray = head(catOptions(array).map(item => item + 10));

// console.log(filteredArray);
