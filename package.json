{
  "name": "root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test:ci",
    "clean": "lerna clean",
    "clean:build": "lerna exec -- rimraf lib",
    "release": "lerna publish",
    "ls": "lerna ls",
    "lerna": "lerna",
    "platform": "cd ex-platform && npm run build",
    "start:client": "npm run lerna -- run build --scope=ex-platform --parallel --scope=ex-client && npm run lerna -- run start"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
